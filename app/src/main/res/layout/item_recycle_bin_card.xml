<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="180dp"
    android:layout_height="120dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    android:layout_margin="8dp"
    app:cardBackgroundColor="#FFEB3B"
    android:id="@+id/recycleCard">
    <!-- bright yellow for visibility -->

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp">

        <!-- Top row: Label (left), Restore (right) -->
        <TextView
            android:id="@+id/recycleLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Diary Entry"
            android:textStyle="bold"
            android:textSize="22sp"
            android:textColor="@android:color/black"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/recycleRestore"
            app:layout_constraintHorizontal_bias="0"
            android:layout_marginEnd="8dp"/>

        <ImageView
            android:id="@+id/recycleRestore"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@android:color/transparent"
            android:backgroundTint="@android:color/transparent"
            android:src="@drawable/ic_restore_bin"
            android:contentDescription="Restore"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="0dp"
            android:clickable="true"
            android:focusable="true"/>

        <!-- Bottom row: Date (left), Delete (right) -->
        <TextView
            android:id="@+id/recycleDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="24 July 2025"
            android:textSize="14sp"
            android:textColor="@android:color/black"
            android:background="@drawable/rounded_gray_box"
            android:paddingLeft="12dp"
            android:paddingRight="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/recycleDelete"
            android:layout_marginEnd="8dp"/>

        <ImageView
            android:id="@+id/recycleDelete"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@android:color/transparent"
            android:backgroundTint="@android:color/transparent"
            android:src="@drawable/ic_dustbin_clean"
            android:contentDescription="Delete Forever"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="0dp"
            android:clickable="true"
            android:focusable="true"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView> 